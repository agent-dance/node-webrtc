default:
  paths:
    - features/**/*.feature
  requireModule:
    - tsx/esm
  require:
    - features/step_definitions/*.ts
  format:
    - progress-bar
    - 'html:reports/cucumber-report.html'
  formatOptions:
    snippetInterface: async-await
  publishQuiet: true
