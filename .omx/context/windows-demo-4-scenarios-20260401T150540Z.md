# Context Snapshot

- task statement: Ensure the current project's demo runs on Windows with the original four-scenario design fully working, with special emphasis on large-file transfer.
- desired outcome: A Windows run can start the signaling server, demo-web, and Flutter app; all four scenarios are enabled and complete successfully without regressing the intended UX/status pages.
- known facts/evidence:
  - README defines four demo scenarios: multi-file, large-file, snake, video.
  - Current demo-web runtime on localhost:3000 only shows Scenario 1 completed; Scenario 2/3/4 remain idle.
  - Current `PeerManager.getEnabledScenarios()` special-cases Windows + flutter-b to only enable scenario 1.
  - Scenario 1/2 code already contains Windows-oriented text-chunk/backpressure options.
  - The Flutter app now includes a Windows target directory and flutter_webrtc 0.13.0.
  - Previous logs under `.tmp/all-scenarios-check` show a prior failure: `DataChannel ... is not open (state: closed)` during file transfers.
- constraints:
  - Preserve original four-scenario design rather than permanently disabling scenarios on Windows.
  - Keep changes small, reversible, and avoid new dependencies.
  - Need fresh verification evidence, not just historical logs.
- unknowns/open questions:
  - Which scenario(s) still fail once all four channels are enabled on Windows.
  - Whether scenario 4 needs fragmentation/throughput adjustments similar to scenario 1/2.
  - Whether current start scripts are sufficient for Windows launch/validation.
- likely codebase touchpoints:
  - `apps/demo-web/src/rtc/peer-manager.ts`
  - `apps/demo-web/src/scenarios/*.ts`
  - `apps/demo-flutter/lib/scenarios/*.dart`
  - `apps/demo-flutter/lib/services/webrtc_service.dart`
  - `apps/demo-flutter/windows/*`
  - `apps/start.sh`, `apps/demo-flutter/start.sh`, `apps/demo-web/start.sh`